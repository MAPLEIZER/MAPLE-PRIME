# CRB registry/evidence layer

Two separate dimensions are mandatory:

- `regulatory_crb_status`: mandatory subscriber / approved third party / unknown;
- `subject_specific_submission_status`: submitted_my_data / no_evidence / disputed / corrected / unknown.

Public-source absence must never be used as proof that a DCP does not submit data to CRBs.
